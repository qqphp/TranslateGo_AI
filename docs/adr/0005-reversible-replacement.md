# ADR-0005：替换翻译采用临时、可恢复的 DOM 覆盖

- 状态：已接受
- 日期：2026-07-26

## 决策

替换模式只临时改写当前页面 DOM：

- 页面刷新后恢复原文。
- 插件提供“恢复原文”操作。
- 用户切换目标语言前，插件自动恢复原文，再基于原文重新翻译。

不把译文永久写回网页，不修改服务器内容，也不把译文作为新的翻译输入。

## 影响

- 必须维护原始文本与译文的可逆映射。
- 翻译任务中途失败时，应能恢复已处理节点。
- 需要避免重复翻译已经插入或替换的内容。
