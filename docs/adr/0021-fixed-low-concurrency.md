# ADR-0021：采用固定并发的请求调度

- 状态：已接受
- 日期：2026-07-26

## 决策

整页翻译使用固定并发调度，当前速度测试最多同时处理 100 个请求。每个节点携带节点 ID，按 ID 缓冲并依页面顺序回写，不依赖响应返回顺序。单批节点数由 ADR-0032 规定。

并发数暂不开放给用户配置。100 并发可能触发供应商限流，后续根据实际性能数据调整默认值或增加配置。
