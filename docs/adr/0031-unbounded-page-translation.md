# ADR-0031：整页翻译不设置请求规模上限

- 状态：已被 ADR-0034 取代
- 日期：2026-07-26

## 决策

MVP 不设置节点数或字符数上限。用户通过右键菜单明确触发整页翻译后，任务可以处理当前页面全部符合范围的文本节点，产生的 API 请求成本由用户自行承担。

## 风险控制

虽然不阻止任务，开始翻译前 UI 应显示待处理节点数量或请求规模提示；用户必须能够取消任务。该决策应在隐私/成本说明中明确，不得将无限规模隐藏在默认自动行为中。
