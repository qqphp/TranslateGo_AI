# ADR-0014：MVP 使用全网网页权限并采用显式数据说明

- 状态：已接受
- 日期：2026-07-26

## 决策

安装时申请 `<all_urls>`，以支持用户在任意普通网页上主动发起整页或选中文本翻译。

隐私说明必须明确：网页内容只在用户主动点击翻译后读取，并只发送到用户配置的 API；插件不默认收集或上传内容。Chrome 受限页面（如 `chrome://` 和 Chrome Web Store）明确不支持。

后续可评估按需 host 权限，但不作为当前 MVP 的隐含要求。
