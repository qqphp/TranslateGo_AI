# ADR-0007：第一版按文本节点逐个请求

- 状态：已被 ADR-0032 取代
- 日期：2026-07-26

## 决策

整页翻译时，第一版按符合范围的可见文本节点逐个发起翻译请求，并将每个响应回写到对应节点。

## 取舍

优点：节点与结果天然一一对应，失败重试、取消和恢复简单，适合先验证页面回写正确性。

缺点：请求数较多，成本和总耗时可能较高，跨节点上下文较弱。

后续可以增加节点聚合/批处理，但必须通过独立设计和回归测试验证结果对齐。
