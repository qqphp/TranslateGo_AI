# ADR-0010：支持多个命名模型配置档案

- 状态：已接受
- 日期：2026-07-26

## 决策

用户可以保存多个命名配置档案，每个档案至少包含 Base URL、API Key、模型名、源语言、目标语言和翻译模式。MVP 支持新增、编辑、删除、测试连接和切换当前档案。

第一版不做按任务自动路由，也不要求整页翻译和选中文本翻译使用不同档案。

## 安全要求

API Key 只在本地保存和使用；列表和普通 UI 不明文展示完整密钥，测试连接和翻译错误不得泄露密钥。
