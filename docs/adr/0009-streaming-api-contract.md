# ADR-0009：第一版要求流式 Chat Completions

- 状态：已被 ADR-0033 取代
- 日期：2026-07-26

## 决策

模型请求采用 OpenAI 兼容 Chat Completions 的流式响应。插件解析 SSE 事件，增量拼接 `choices[0].delta.content`，并在当前文本节点完成时回写译文。

## 风险

不同兼容端点的 SSE 格式和错误行为可能不一致。实现必须处理连接中断、空事件、非标准结束标记和响应错误；是否兼容非流式响应暂不在本决策内，需后续单独确定。
