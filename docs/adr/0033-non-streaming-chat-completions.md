# ADR-0033：采用非流式 Chat Completions 响应

- 状态：已接受
- 日期：2026-07-26

## 决策

选中文本和整页批量翻译均使用 OpenAI 兼容 Chat Completions 的普通 JSON 响应（`stream: false`）。整页批量译文须完整返回后才能校验节点 ID、保持页面顺序并回写，因此 SSE 增量并无用户可见收益。

## 影响

- 减少 SSE 解析与事件协议开销，并降低兼容端点的流式实现差异。
- 译文在单次请求完成后显示；整体速度主要仍受模型推理、批量大小和并发数影响。
- ADR-0009 被本决策取代。
