# ADR-0022：节点筛选不按文本内容做语义过滤

- 状态：已接受
- 日期：2026-07-26

## 决策

第一版只根据 DOM 元素类型、文本节点可见性和既定排除元素筛选翻译节点。不根据文本是否为纯数字、URL、邮箱、短文本或其他内容特征过滤。

## 风险

技术文本和不需要翻译的内容可能产生额外 API 请求与成本。该风险接受到 MVP，后续应通过真实网页采样评估，再设计可解释的过滤规则。
